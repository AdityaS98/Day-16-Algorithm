package com.AlgorithmProblems;

import java.util.*;
/**
 * 
 * @author DELL
 *
 */

public class RangePrimeNumber {
	/**
	 * 
	 * @param rangeOfNumber
	 */

	static void toFindPrimeNumber(int rangeOfNumber) {
		/*
		 * A function to claculate range and the numbers that are prime in it 
		 */

		int k = 0;

		for (int i = 2; i <= rangeOfNumber; i++) {
			int count = 0;
			for (int j = 2; j <= i; j++) {
				if (i % j == 0) {
					count++;
				}
			}
			if (count == 1) {
				System.out.println(i);
			}
		}
	}
/**
 * 
 * @param args
 */
	public static void main(String[] args) {
		
		Scanner sc = new Scanner(System.in);

		System.out.println("Enter range for numbers for which you want to calculate");

		int rangeOfNumber = sc.nextInt();

		/*
		 * To call out method
		 */

		toFindPrimeNumber(rangeOfNumber);

	}

}
